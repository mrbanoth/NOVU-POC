# Screenshots

Drop the 4 PNGs here with these exact names (they are referenced by the main README):

| File | What it shows |
|---|---|
| `01-push-notification.png` | Employee dashboard with a real Chrome desktop **push notification** (our own Web Push / VAPID) |
| `02-inapp-bell.png` | Tenant Admin with the in-app **bell** dropdown of real-time notifications |
| `03-novu-activity-feed.png` | Novu dashboard **Activity Feed** — every notification as a workflow run |
| `04-novu-subscribers.png` | Novu **Subscribers** — tenant-isolated composite ids (`tenant:user`) |

To add them: save each screenshot as the filename above into this folder, then
`git add docs/screenshots && git commit -m "Add screenshots" && git push`.
